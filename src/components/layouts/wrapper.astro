---
import "../../styles/globals.css";

import Metadata from "./metadata.astro";
import Navbar from "./navbar.astro";
import Footer from "./footer.astro";

type Props = {
  title?: string;
  description?: string;
  opengraphImage?: string;
};

const { title, description, opengraphImage } = Astro.props;
---

<!doctype html>
<html lang="pt-br" class="overflow-x-hidden">
  <Metadata
    title={title}
    description={description}
    opengraphImage={opengraphImage}
  />

  <body
    class={"relative mx-auto flex min-h-screen min-h-dvh max-w-[800px] flex-col gap-8 overflow-x-hidden bg-background px-6 lg:px-0"}
  >
    <div
      class="pointer-events-none absolute -top-24 left-0 z-[-1] h-80 w-full max-w-[100vw] -translate-x-1/3 rotate-45 rounded-full bg-slate-900/28 blur-3xl lg:h-[28rem]"
    >
    </div>
    <Navbar />
    <div class="flex-1">
      <slot />
    </div>
    <Footer />
  </body>
</html>
